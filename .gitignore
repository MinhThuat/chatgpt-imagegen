# Python
__pycache__/
*.py[cod]
*.egg-info/

# OS
.DS_Store

# Generated images — never commit by accident
assets/generated/
*.png
*.jpg
*.jpeg
*.webp

# Keep README/docs images explicitly (use git add -f if needed)
!docs/**/*.png
!docs/**/*.jpg
!docs/**/*.webp

# Personal drafts / promo material — not part of the published skill
drafts/

# User-specific generated scripts (created per session by Claude Code)
run_*.ps1

# Output folders (generated images)
out_*/

# Personal reference images (each user has their own)
anh_tham_chieu/

# chrome-use binaries (platform-specific, download separately)
chrome-use-bin/
*.tar.gz
